# --- Secrets ---
# File with real API keys. Template — .env.example.
.env
.env.local
*.pem
*.key

# --- Go compiler binaries ---
/bin/
/build/
*.test
*.out
coverage.out
coverage.html

# Accidentally produced binaries in the project root.
# Occur when running `go build ./examples/<name>` from the root without the -o flag.
# The filename matches the last segment of the package path.
/account-info
/inventory-monitor
/inventory-tracker
/market-data
/orderbook-watcher
/public-streams
/simple-trade

# --- IDE / OS junk ---
.vscode/
.idea/
.DS_Store
Thumbs.db
